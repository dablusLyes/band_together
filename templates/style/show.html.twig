{% extends 'dashboard.html.twig' %}

{% block title %}Style{% endblock %}

{% block body %}
    <h1>Style</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ style.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ style.nom }}</td>
            </tr>
            <tr>
                <th>Categoerie</th>
                <td>{{ style.categoerie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('style_index') }}">back to list</a>

    <a href="{{ path('style_edit', {'id': style.id}) }}">edit</a>

    {{ include('style/_delete_form.html.twig') }}
{% endblock %}
