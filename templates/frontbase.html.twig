<!DOCTYPE html>
<html>
<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="reply-to" content="contact@bandtogether.fr">
    <meta name="description" content="Trouvez en quelques clics des musiciens autour de vous pour jouer ensemble !">

    <title>{% block title %}Band Together{% endblock %}</title>

    {% block stylesheets %}{% endblock %}

</head>

<body id="top">

        <header id="header">

                {#<div class="logo_container">#}
                {#<img src="{{ asset('assets/img/logo.png') }}" alt="logo band together" class="logo">#}
                {#</div>#}
        
                <nav>
                    <ul>
                        <li><a href="{{ path('home')}}#top">Accueil</a></li>
                        <li><a href="{{ path('home')}}#about">A propos</a></li>
                        <li><a href="{{ path('home')}}#news">News</a></li>
                        <li><a href="{{ path('home')}}#contact">Contact</a></li>
                        {% if (is_granted('ROLE_ADMIN') == true) %}
                            <li><a href="{{ path('dashboard') }}">Admin</a></li>
                        {% endif %}
                        {% if  (is_granted('ROLE_USER') == false) and (is_granted('ROLE_ADMIN') == false)  %}
                            <li><a href="{{ path('app_login') }}">Connexion</a></li>
                            <li><a href="{{ path('register') }}">Inscription</a></li>
                        {% else %}
                            <li><a href="{{ path('profile') }}">Profil</a></li>
                            <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                        {% endif %}
                    </ul>
                </nav>
        
            </header>

{% block body %}
{% endblock %}



{% block javascripts %}
    <script src="http://code.jquery.com/jquery-3.3.1.min.js"
            integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>
    <script src="{{ asset('assets/js/main.js') }}"></script>
    <!-- Google Analytics -->
    {#<script async src="https://www.googletagmanager.com/gtag/js?id=UA-136670597-1"></script>#}
    {#<script>#}
    {#window.dataLayer = window.dataLayer || [];#}
    {#function gtag(){dataLayer.push(arguments);}#}
    {#gtag('js', new Date());#}

    {#gtag('config', 'UA-136670597-1');#}
    {#</script>#}
{% endblock %}

</body>
</html>


